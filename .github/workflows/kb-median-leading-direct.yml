name: Direct KB median Leading50
on:
  workflow_dispatch:
  push:
    paths: ['scripts/probe_kb_median_leading_direct.py','.github/workflows/kb-median-leading-direct.yml']
permissions: {contents: write}
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python3 scripts/probe_kb_median_leading_direct.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f data_sources/kb_median_leading_direct.json
          git commit -m 'Probe direct KB median Leading50 APIs' || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main || true
